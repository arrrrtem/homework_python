# Напишите функцию которая принимает имя файла в качестве аргумента, имя файла передается скрипту как параметр
# если файл доступен на чтение функция возвращает количество строчек в этом файле, если нет возвращает -1
# PS для подсчета количества строчек есть команда l *имя файла* или wc -l *имя файла *

#!/bin/bash

count_lines() {
    filename=$1

    # Проверка доступности файла на чтение
    if [ -r "$filename" ]; then
        # Используем команду wc -l для подсчета строк в файле
        lines=$(wc -l < "$filename")
        echo "Количество строк в файле: $lines"
    else
        echo "Ошибка: Файл не доступен на чтение."
        return -1
    fi
}

# Пример использования функции с передачей имени файла в качестве аргумента
filename=$1
count_lines "$filename"
