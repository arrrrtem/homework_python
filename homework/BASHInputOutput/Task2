# Напишите скрипт который перенаправляет вывод данных из всех файлов этой директории в файл done
# в конце в консоль выводится надпись о завершении работы

#!/bin/bash

# Перенаправление вывода из всех файлов в директории в файл done
cat * > done

# Вывод надписи о завершении работы
echo "Завершено. Весь вывод из файлов сохранен в done."
